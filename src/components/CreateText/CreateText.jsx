import React from "react";

const CreateText = ({ text }) => {
  return (
    <div>
      <p class="text-center text-gray-500 dark:text-gray-400">{text}</p>
    </div>
  );
};

export default CreateText;
